{
  "name": "ai-code-reviewer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "smoke-test": "bash scripts/smoke-test.sh",
    "test:e2e": "node --env-file=.env scripts/e2e-smoke-test.js",
    "bootstrap": "echo 'Bootstrap complete'",
    "build:all": "echo 'Building all services...'",
    "dev:all": "echo 'Starting all services in dev mode...'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

